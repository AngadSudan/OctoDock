import { promptResolver } from "./prompt.resolver";
import { promptDef } from "./prompt.def";
import { promptMutation } from "./prompt.mutations";
import { promptQueries } from "./prompt.queries";

export const Prompt = {
  promptResolver,
  promptDef,
  promptMutation,
  promptQueries,
};
